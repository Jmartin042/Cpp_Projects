#include "p1.h"
// example program: main1_2.cc
int main ()
{
        SIMPLER_GRADES g(4,3);  // create an object g of class SIMPLER_GRADES 
				// which has 4 students each with 3 grades;
	g.SIMPLER_LIST(2); // list grades for exam 2 previously: g.SIMPLER_LIST(2);
        return 0;
}
