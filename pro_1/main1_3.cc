#include "p1.h"
// example program: main1_3.cc
int main ()
{
        SIMPLER_GRADES g(4,3);  // create an object g of class SIMPLER_GRADES 
				// which has 4 students each with 3 grades;
	g.SIMPLER_LIST(-1); // list all grades for all students;
        return 0;
}
