#include "p3.h"
// example program: main3_1.cc
int main ()
{
        GRADES g(4,3);  // create an object g of class SIMPLE_GRADES 
				// which has 4 students each with 3 grades;
	g.SIMPLER_LIST(-1); // list all exams using base class call; 

        return 0;
}
